#pragma once

enum crkbd_layers {
    BASE = 0,
    QWERTY,
    GAME,
    NAV,
    GAME_NAV,
    SYMBOL,
    MOUSE,
    ADJUST,
    NUMPAD
};
enum custom_keycodes {
    SW_WIN = SAFE_RANGE,
    SW_SWIN,
    SW_CTAB,
    SW_SCTAB,
    LLOCK
};
