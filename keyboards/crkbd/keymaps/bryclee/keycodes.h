#pragma once

enum crkbd_layers { QWERTY = 0, COLEMAK, NAV, SYMBOL, NUM, ADJUST, NUMPAD };
enum custom_keycodes {
    SW_WIN = QK_USER,
    SW_SWIN,
    SW_CTAB,
    SW_SCTAB,
    OS_SHFT,
    OS_CTRL,
    OS_ALT,
    OS_GUI,
    LLOCK,
    REPEAT,
    REVREPEAT
};
